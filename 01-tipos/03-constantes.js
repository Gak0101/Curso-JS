const texto = "Ramon";

texto = "Ramoncito"; // esto no funciona ya que se ha declarado uanc onstante y no puede cambair esa variable,
// en cambio si fuera let, si se podria mdoificar, de esta manera se asegura que una variable cambie o no cambie

console.log (texto);
